# x402 AI Agent Demo コードリーディング

---

## 1. フォルダ構成

```
x402-demo-2026-0406/
├── provider/                       # 天気APIを提供するサーバー（:3001）
│   └── src/
│       ├── index.ts                # サーバー起動・x402ミドルウェア設定
│       ├── routes/
│       │   └── weather.ts          # 無料/プレミアム天気エンドポイント
│       └── services/
│           └── mockWeather.ts      # 天気データ生成（モック）
│
├── agent/                          # AIエージェントサーバー（:3002）
│   └── src/
│       ├── index.ts                # サーバー起動・環境変数チェック
│       ├── routes/
│       │   └── agent.ts            # エージェントのメインロジック
│       └── services/
│           ├── aiDecision.ts       # Claude APIで判断
│           └── walletBalance.ts    # ウォレット残高取得
│
└── frontend/                       # クライアントサイド（React）:5173
    └── src/
        ├── main.tsx                # Reactエントリーポイント
        ├── App.tsx                 # 全体レイアウト・状態管理
        ├── types.ts                # TypeScript型定義
        └── components/
            ├── InputForm.tsx           # 入力フォーム
            ├── ResultDisplay.tsx       # 結果表示
            ├── LogDisplay.tsx          # エージェントログ
            └── CityWeatherAccordion.tsx # 都市天気一覧
```

---

## 2. 処理の流れ

```
[フロントエンド]
  InputForm → Run Agent クリック
      ↓
  App.tsx → POST /api/agent/run（→ Agent :3002 にプロキシ）

[Agent Server]
  routes/agent.ts
    ├── 1. Provider の /weather/free を fetch（無料天気取得）
    ├── 2. services/aiDecision.ts → Claude APIで buy/skip 判断
    └── 3. buy の場合
          ├── @x402/fetch で Provider の /weather/premium にリクエスト
          ├── Provider の x402ミドルウェアが 402 を返す
          ├── @x402/fetch が自動でUSDC支払い
          └── 支払い確認後にプレミアム天気を返却

[フロントエンド]
  App.tsx → 結果をstateに保存
    ├── ResultDisplay → 結果表示
    └── LogDisplay    → ログ表示
```

---

## 3. ファイル別コード解説

---

### `provider/src/index.ts` — x402ミドルウェア設定

```typescript
import { paymentMiddleware, x402ResourceServer } from '@x402/express';
import { ExactEvmScheme } from '@x402/evm/exact/server';
import { HTTPFacilitatorClient } from '@x402/core/server';
```

**x402関連の3つのクラスをインポートしている。**

- `paymentMiddleware`：Expressのミドルウェア。指定したルートへのリクエストに対して支払いを要求する
- `x402ResourceServer`：支払いを受け付けるサーバーの設定
- `HTTPFacilitatorClient`：支払いの検証を外部のFacilitatorに委託するクライアント
- `ExactEvmScheme`：EVMチェーン（Base Sepolia）での支払いスキーム

```typescript
app.use(
  paymentMiddleware(
    {
      'GET /weather/premium': {       // ← 保護するルート
        accepts: [{
          scheme: 'exact',
          price: '$0.01',             // ← 価格
          network: 'eip155:84532',    // ← Base SepoliaのチェーンID
          payTo: EVM_ADDRESS,         // ← 受取アドレス
        }],
      },
    },
    new x402ResourceServer(facilitatorClient)
      .register('eip155:84532', new ExactEvmScheme()),
  ),
);
```

**ポイント：**
`/weather/premium` にリクエストが来ると、支払い証明があるかをチェックする。
なければ `402 Payment Required` を返し、あればFacilitatorで検証して通過させる。

---

### `provider/src/config.ts` — 価格定数

```typescript
export const PREMIUM_PRICE = 0.01;
```

**価格はProviderが一元管理。** `provider/src/index.ts`（paymentMiddleware）と `provider/src/routes/weather.ts`（`/price` エンドポイント）の両方がここから参照する。

---

### `agent/src/routes/agent.ts` — エージェントのメインロジック

このファイルが一番重要。AIエージェントの「判断 → 支払い → データ取得」を実装している。

```typescript
router.get('/price', async (_req, res) => {
  const r = await fetch(`${PROVIDER_URL}/weather/price`);
  res.json(await r.json());
});
```

**AgentはProviderから価格を取得してフロントエンドに返す。** 価格をAgentが持たず、Providerに問い合わせることで一元管理を実現している。

```typescript
router.get('/balance', async (_req, res) => {
  const balance = await getUsdcBalance();
  res.json({ balance });
});
```

**ウォレットのUSDC残高をフロントエンドに返す。** フォームの「Budget」表示に使われる。

```typescript
router.post('/run', async (req, res) => {
  const { location, urgency, budget, overridePrice } = req.body;
  // Step 0: Providerから価格取得
  const { price: fetchedPrice } = await fetch(`${PROVIDER_URL}/weather/price`).then(r => r.json());
  const priceForClaude = overridePrice ?? fetchedPrice;
```

**`overridePrice`** は「予算超過シナリオ」チェックボックス用。`999`が渡されるとClaudeは「高すぎる」と判断してSkipする。

```typescript
// Step 1: Provider の無料天気を取得
const freeRes = await fetch(`${PROVIDER_URL}/weather/free?location=...`);

// Step 3: 自律支払い
const evmSigner = privateKeyToAccount(privateKey);  // 秘密鍵 → ウォレットオブジェクト
const client = new x402Client();
client.register('eip155:*', new ExactEvmScheme(evmSigner));
const fetchWithPayment = wrapFetchWithPayment(fetch, client);
```

**`wrapFetchWithPayment`** がこのデモの核心。通常の `fetch` をラップして、
`402` を受け取ったときに自動で支払いを実行して再リクエストする `fetch` を返す。

```typescript
const premiumResponse = await fetchWithPayment(premiumUrl);
// ↑ これだけで「402受信 → 支払い → 再リクエスト」が自動で行われる
```

---

### `agent/src/services/aiDecision.ts` — Claude APIによる判断

```typescript
const anthropic = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });
```

**APIキーは環境変数から取得。** コードに直書きしないのがセキュリティの基本。

```typescript
const message = await anthropic.messages.create({
  model: 'claude-haiku-4-5-20251001',  // コスト削減のためHaikuモデルを使用
  max_tokens: 200,                      // 短い返答しか必要ないので小さく設定
  messages: [{ role: 'user', content: prompt }],
});
```

```typescript
const raw = content.text.trim()
  .replace(/^```json\s*/i, '')   // Claudeが ```json で囲む場合の対処
  .replace(/```\s*$/, '')
  .trim();
const parsed = JSON.parse(raw);
```

**Claudeは `{"buy": true, "reason": "..."}` というJSONを返す。**
ただし稀にマークダウンのコードブロックで囲んで返すことがあるため、除去してからパースしている。

---

### `provider/src/services/mockWeather.ts` — 天気データ生成

```typescript
function getCondition(location: string): WeatherCondition {
  const index = (location.length * location.charCodeAt(0)) % conditions.length;
  return conditions[index];
}
```

**都市名の「文字数 × 先頭文字コード」を6で割った余りで天気を決定する。**

```
Tokyo: 5文字 × 84(T) = 420, 420 % 6 = 0 → 快晴
Paris: 5文字 × 80(P) = 400, 400 % 6 = 4 → 嵐
```

このアルゴリズムにより：
- 同じ都市名は常に同じ天気（デモの再現性）
- 外部APIに依存しない（ネット不要・高速）
- 都市名を変えるだけで全6種類の天気を試せる

---

### `agent/src/services/walletBalance.ts` — USDC残高取得

```typescript
const publicClient = createPublicClient({
  chain: baseSepolia,
  transport: http(),
});
```

**`createPublicClient`（viem）** はブロックチェーンへの読み取り専用クライアント。
秘密鍵なしでオンチェーンのデータを読める。

```typescript
const raw = await publicClient.readContract({
  address: USDC_ADDRESS,  // Base SepoliaのテストUSDCコントラクト
  abi: erc20Abi,          // ERC20の標準ABI
  functionName: 'balanceOf',
  args: [account.address],
});

return Number(raw) / 1_000_000;  // USDCは小数点6桁
```

**`readContract`** でUSDCコントラクトの `balanceOf` 関数を呼び出す。
返ってくる値は `1000000 = 1.0 USDC` なので `1_000_000` で割る。

---

### `provider/src/routes/weather.ts` — 天気エンドポイント

```typescript
router.get('/free', (req, res) => {
  const location = (req.query.location as string) || 'Tokyo';
  res.json(getFreeWeather(location));
});

// x402ミドルウェアは index.ts で設定済み
router.get('/premium', (req, res) => {
  const location = (req.query.location as string) || 'Tokyo';
  res.json(getPremiumWeather(location));
});
```

**シンプルな2つのエンドポイント。**
`/premium` への保護は `index.ts` のミドルウェアが担当しているため、
このファイルは「通過したリクエストにデータを返すだけ」のシンプルな実装になっている。

---

### `frontend/src/App.tsx` — 全体の状態管理

```typescript
const [status, setStatus] = useState<Status>('idle');
// 'idle' | 'thinking' | 'paying' | 'done' | 'error'
```

**アプリ全体の状態をここで管理。** 状態に応じてステータスインジケーターの色が変わる。

```typescript
if (data.paid) {
  setStatus('paying');
  await new Promise((r) => setTimeout(r, 800));  // 支払い中の演出
}
```

**支払いが発生した場合、0.8秒だけ「Paying...」状態を表示する。**
実際の支払い処理はすでに終わっているが、デモとして視覚的に見せるための演出。

---

### `frontend/src/components/InputForm.tsx` — 入力フォーム

```typescript
useEffect(() => {
  fetch('/api/agent/price').then(r => r.json()).then(d => setActualPrice(d.price));
  fetch('/api/agent/balance').then(r => r.json()).then(d => setBalance(d.balance));
}, []);
```

**画面表示時に一度だけ** バックエンドから価格とウォレット残高を取得する。
`[]` は「マウント時1回だけ実行」を意味する React の useEffect の書き方。

```typescript
const handleSubmit = () => {
  onSubmit({
    location,
    urgency,
    budget: balance ?? 0.1,         // 残高取得失敗時は0.1をデフォルトに
    overridePrice: overScenario ? 999 : undefined,  // シナリオON時は999を渡す
  });
};
```

---

### `frontend/src/components/ResultDisplay.tsx` — 結果表示

```typescript
const weatherIcon: Record<string, string> = {
  clear: '🌞', sunny: '☀️', cloudy: '☁️',
  rainy: '🌧️', stormy: '⛈️', tornado: '🌪️',
};
const weatherJa: Record<string, string> = {
  clear: '快晴', sunny: '晴れ', cloudy: '曇り',
  rainy: '雨', stormy: '嵐', tornado: '竜巻',
};
```

**バックエンドからは英語（`sunny`など）で返ってくる。**
フロントエンドで日本語・絵文字に変換している。

---

### `frontend/src/components/LogDisplay.tsx` — ログ表示

```typescript
const colorMap: Record<string, string> = {
  '[Agent]':    '#f0c040',  // 黄（Agent Serverの処理）
  '[Claude]':   '#a78bfa',  // 紫（Claude APIの判断）
  '[Provider]': '#34d399',  // 緑（Provider Serverのレスポンス）
  '[Error]':    '#f05040',  // 赤（エラー）
};

function lineColor(line: string): string {
  for (const [prefix, color] of Object.entries(colorMap)) {
    if (line.startsWith(prefix)) return color;
  }
  return '#888';  // 該当なしはグレー
}
```

**ログの先頭文字列を見て色分けする。**
`[Agent]`・`[Claude]`・`[Provider]` でそれぞれ誰の発言かが一目でわかるようにしている。

---

### `frontend/src/types.ts` — 型定義

```typescript
export interface AgentRequest {
  location: string;
  urgency: Urgency;
  budget: number;
  overridePrice?: number;   // ← ? は省略可能を意味する
}

export interface PremiumWeather {
  temperature: number;
  rain_probability: number;
  detail: string;
  advice: string;
}
```

**TypeScriptの型定義ファイル。**
フロントエンドとバックエンドのデータ構造をここで定義しておくことで、
型ミスをコンパイル時に検出できる。

---

## 4. x402の仕組みまとめ

```
通常のHTTP                          x402
─────────────────────────────────────────────────────
Client → GET /premium              Client → GET /premium
Server → 200 OK（即返却）           Server → 402 Payment Required
                                              + 支払い情報（金額・アドレス・チェーン）
                                   Client → USDCを送金
                                   Client → GET /premium（支払い証明付き）
                                   Facilitator → 支払い検証
                                   Server → 200 OK（データ返却）
```

**x402のポイント：**
- HTTP標準の `402` ステータスコードを活用している
- 支払い処理が `wrapFetchWithPayment` で完全に自動化されている
- サーバー側は `paymentMiddleware` を追加するだけでAPIを有料化できる

---

## 5. 主要パッケージ一覧

| パッケージ | 役割 |
|-----------|------|
| `@x402/express` | Expressで402ミドルウェアを提供（Provider側） |
| `@x402/fetch` | fetchを拡張して自動支払いを実現（Agent側） |
| `@x402/evm` | EVMチェーンの支払いスキーム |
| `@anthropic-ai/sdk` | Claude APIクライアント（Agent側） |
| `viem` | Ethereumウォレット操作・コントラクト読み取り |
| `express` | Node.js HTTPサーバーフレームワーク |
| `dotenv` | `.env` ファイルを環境変数として読み込む |
| `react` | UIコンポーネントフレームワーク |
| `vite` | 高速なフロントエンドビルドツール |
